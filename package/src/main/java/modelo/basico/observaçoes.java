package modelo.basico;

import javax.persistence.Entity;
import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

//import javax.persistence.EntityManagerFactory;
//import javax.persistence.Persistence;

public class observaçoes {
	
// JPQL == SQL so que totalmente voltado a OBJETO
	
/*EntityManager== ele é responsavel CRUDE ele é a INTERFACE ne perssiste os dados no BD
e tb tem faz a conexao ao BD
EntityManagerFactory== cria um EntityManager, ex:  EntityManager em = emf.createEntityManager();  
EntityManagerFactory emf = Persistence.createEntityManagerFactory("exercicios-jpa"); ==
esse foi criado pra essa unidade de persistencia ("exercicios-jpa") caso queira pra outro BD
so criar outro EntityManager ou seja pra cada unidade de persistencei (cada BD) um dele é criado 

SELECT== usuario usuario = em.find(usuario.class, 3L); find + refencia a classe + PK(id);
		 System.out.println(usuario.getNome());
		 System.out.println(usuario.getEmail());
===========================CONEXAO PADRAO NO PERSISTENCE============================
		"EntityManagerFactory emf = Persistence.createEntityManagerFactory("exercicios-jpa");
		 EntityManager em = emf.createEntityManager();"
		 
		 
		 
		 
*/

	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
}
